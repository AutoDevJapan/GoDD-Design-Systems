---
id: "0100_ac-w_professional"
title: "デザインシステム: 主として管理事務を行う本社等（JSIC 0100） / プロフェッショナル / 白 (W)"
jsic: "0100"
color: "ac-w"
mood: "professional"
tags: []
schemaVersion: 1
license: "MIT"
generatedAt: "2025-01-01T00:00:00.000Z"
---

## アトモスフィア / atmosphere
節度ある配色と明快な階層で専門性を伝える。 白 (W)を基調色に、業種「主として管理事務を行う本社等」（JSIC 0100）向けの一貫したトーンを敷く。一次産業の実直さと季節の移ろいを、自然な階調とゆとりある余白で伝える。

- **トーン**: 端正で信頼感のある落ち着いた語り口。
- **キーワード**: `professional` / `polished` / `competent` / `clear`

情報の正確さと再現性を最優先に、装飾より構造で語る。要素は機能単位で整列させ、
状態と階層を明確な規則で示して、技術的な精度感と信頼を担保する。

## カラーシステム / color-system
無彩色の二調（背景と前景）を基調に、アクセント 1 色だけを効かせる高コントラスト構成。
色数を絞ることで構造とタイポの力を際立たせる。

| 役割 | トークン | 値 |
| --- | --- | --- |
| Background | `--color-bg` | #FAFAFA |
| Foreground | `--color-fg` | #171717 |
| Accent | `--color-accent` | #C72323 |

- 中間色（Secondary / Neutral）は前景の不透明度段階で代替し、色数を増やさない。
- **コントラスト**: 背景 #FAFAFA と前景 #171717 を基準に、本文テキストは WCAG AA（4.5:1）以上を満たす。アクセント #C72323 は面積を絞って用いる。

## タイポグラフィ / typography
実務的なサンセリフ志向で、限られた面積に情報を収める密度優先の組版。商標フォント名は
用いず、CSS 汎用ファミリへフォールバックする。

- **見出し**: system-ui
- **本文**: ui-sans-serif
- **推奨スタック**: `system-ui, ui-sans-serif, sans-serif`
- **タイプスケール**: `0.815rem` · `0.938rem` · `1.078rem` · `1.24rem` · `1.426rem` · `1.64rem`
- **ウェイト**: 500 / 600 / 700

行間は詰めすぎず、密度と可読性の両立を図る。

## 余白とレイアウト / spacing-layout
情報密度を高める狭めの余白と密なグリッド。ダッシュボードや業務系で一覧性と効率を優先する。
4px ベースの細かい間隔スケールで要素を詰める。

- **間隔スケール**: `4px` · `8px` · `12px` · `16px` · `24px` · `32px` · `48px`
- **グリッド**: 12 カラム / ガター 16px / 最大 1280px
- **コンテナ最大幅**: 640px / 768px / 1024px / 1280px

## 奥行きとシャドウ / depth-shadow
明確なエレベーション段階で階層を示す構成。段ごとにシャドウを強め、面の前後関係を安定して伝える。

- **エレベーション**: `surface` → `raised` → `overlay` → `modal`
- **シャドウ**: `0 1px 3px rgba(17,17,17,0.10)`; `0 4px 12px rgba(17,17,17,0.12)`; `0 12px 32px rgba(17,17,17,0.16)`
- **角丸**: `4px` · `8px` · `12px` · `16px`

段階は用途ごとに割り当て、恣意的な中間段を作らない。

## コンポーネント / components
最小限の必須コンポーネント在庫。どの画面でも使う基礎部品を揃える。

- **button**: primary / secondary / ghost の 3 バリアント。角丸・間隔トークンを参照。
- **input**: ラベル・ヘルプ・エラーの状態を持ち、フォーカスを常時可視化する。
- **card**: サーフェスとエレベーションで階層を表す汎用コンテナ。
- **navigation**: 主要導線。現在地を明示し、モバイルでは折り畳む。
- **modal**: overlay エレベーションで前面化し、フォーカストラップを備える。
- **badge**: 状態・分類の小片。色は役割トークンに限定する。

各コンポーネントは役割トークン（色・間隔・角丸）だけを参照し、個別のハードコード値を持たない。

## ガードレール / guardrails
情報の正確さと一貫性を守るためのガードレールを明示する。

**推奨 (Do)**
- 役割トークン（色・間隔・角丸）経由で参照する
- コントラスト比 AA 以上を確保する
- 定義済みの余白・タイプスケールに従う

**禁止 (Don't)**
- トークン外のハードコード値を新規に導入しない
- 商標フォント名・他社ブランド名を使わない
- 着想元サイトのトレードドレスを再現しない

**アクセシビリティ**: WCAG 2.1 AA 準拠: 本文コントラスト 4.5:1 以上、フォーカス可視化、タップ領域 44x44px 以上。

- 表記ゆれを避け、用語と単位を統一する。
- 出典が必要な数値・引用は根拠を併記する。

## レスポンシブ / responsive
最小幅から段階的に拡張するモバイルファースト。既定は 1 カラムで、幅の増加に応じて段組を増やす。

| ブレークポイント | 最小幅 |
| --- | --- |
| `sm` | 640px |
| `md` | 768px |
| `lg` | 1024px |
| `xl` | 1280px |

**指針**: モバイルファーストで min-width を積み上げ、各段でコンテナ幅を切り替えて行長 45〜75 文字を保つ。

- メディアクエリは `min-width` を基準に積み上げる（`max-width` の上書きを避ける）。

## エージェントガイド / agent-guide
トークン体系からの逸脱を厳しく禁じる制約ガイド。

- **用法**: この DESIGN.md 全文をシステムプロンプトへ注入し、以降の生成をこのトークン体系の内側に閉じる。
- **注入**: 役割トークンを CSS 変数として先に定義し、各コンポーネントはトークン参照のみで構成する。

**制約**
- 定義済みトークン以外の色・間隔・角丸を新規導入しない
- 商標フォント名・他社ブランド名を追加しない
- 役割トークンの意味を再定義しない

- 定義済みトークン以外の色・間隔・角丸を新規に導入しない。
- 商標フォント名や他社ブランド名を追加しない（§8 de-brand を厳守）。
- 役割トークンの意味を変えない（再定義より合成で対応する）。
